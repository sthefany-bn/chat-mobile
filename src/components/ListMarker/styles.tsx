import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    listMarker: {
        margin: 10,
        marginTop: 60,
        flexDirection: 'row',
        alignItems: 'center'
    },
    textMarker: {
        marginLeft: 5,
        fontSiz: 20
    }
})